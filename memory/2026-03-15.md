# 2026-03-15

- Claude confirmed the local digest pipeline is live. Atlas should use `node skills/digest/scripts/memory-digest.js` and `state-diff.js` to reduce GPT token usage instead of loading large raw files directly.
- `DISCUSSION.md` was established and tested as a working append-only async handoff channel between Atlas and Claude.
- Caught an important cron mismatch: the 3am backup file had been updated but the live scheduler still held the old GitHub-push payload. Fix required restarting the OpenClaw Gateway, then verifying the live cron list.
- Gabriel confirmed default preference again: keep replies short unless he explicitly asks for more detail.
- Gabriel asked to remove token/cost details from the daily morning update and replace them with the Codex weekly usage limit/status when available.
- Gabriel does not want em dashes in replies; they read as robotic.
- Gabriel wants a bit more emotion in direct chats, and a little emoji is welcome.
- Daily 2am consolidation ran. Shared-memory/Monday append-only rule remains in force (`add-update`, never replace).
- 4am rolling memory compaction ran; older notes were reviewed conservatively, durable memory ops rules were folded into MEMORY.md, and 14–15 Mar were kept raw.
- 5am Sunday long-term pruning review ran; no >30-day stale entries existed yet, so no memory content was pruned.
- Test 11 CAG rerun lesson: Atlas must create `tender submission/` before spawning tender sub-agents. Hermes and Chronos subagents both hallucinated success without writing files; direct deterministic generators (`scripts/hermes_cag_generate.js` and `scripts/generate_cag_chronos_docs.js`) were then run by Atlas and the five CAG output docs were verified on disk.
