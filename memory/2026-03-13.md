# 2026-03-13

- Scheduled a one-time 8:00am Singapore-time morning recap for Gabriel on 2026-03-14. The recap should summarize the previous day’s work, decisions, Gateway issues, WhatsApp disconnects, and token/cost usage if available.
- Gabriel wants a useful daily-style morning summary and plans to iterate on the content after the first run.
- Corrected the recap scheduling mistake: replaced the one-time 2026-03-14 reminder with a recurring daily 8:00am Asia/Singapore morning recap so Gabriel can tell Atlas is up.
- Gabriel prefers Atlas to call herself she/her.

## Sessions 11–12 (afternoon)

- Gateway outage: fixed by recreating Scheduled Task XML with RestartCount=999, RestartInterval=PT1M, StartWhenAvailable=True.
- Hermes first CAG run confirmed on gpt-5.4 (not qwen3:32b). AGENTS.md updated with explicit model routing rule. ISSUES.md created with 5 issues.
- Workspace backup taken: workspace-backup-2026-03-13 (108MB).
- Project site updated: Hermes profile + amber theme + Agent Fleet Status + Phase 04 + Session 11 log. Pushed to GitHub.
- Chronos SKILL.md built: PMP sub-agent covering Section 8 (Project Plan), Section 9 (Governance), Section 10 (Team/CVs). References: Schedule 6 template, risk register template (10 risks), ICA AQS PMP structure. 3 evals written.
- PMO CPIB past submission PMP inaccessible (OneDrive not synced) — built from CAG Appendix K + ICA AQS PMP instead.
- Project site updated: Chronos team card (purple), Skills Roadmap badge, Phase 04 checklist, Session 12 log. Pushed (7f7cb71).

## Sessions 13–14 (late afternoon)

- Gabriel asked for a dual rerun of CAG using Hermes + Chronos and explicitly asked which model each would use.
- Atlas confirmed both should run on `ollama/qwen3:32b` but launched them incorrectly: the instruction was placed in prompt text while the `sessions_spawn` top-level `model` parameter was omitted.
- Result: both Hermes rerun and Chronos live run actually executed on `gpt-5.4`, not Ollama. Gabriel noticed because Ollama activity was absent.
- Gabriel estimated the mistaken GPT-5.4 run cost about **$1.81** and told Atlas she "owns" it.
- Root-cause confirmed by inspecting both subagent histories: provider/model showed OpenAI Codex `gpt-5.4` for Hermes and Chronos.
- Corrective action recorded: for future subagent runs, Atlas must set the explicit `model` field on `sessions_spawn` (not just mention the model in prompt text).
- Gabriel will back up the GPT-5.4 outputs to compare later. Atlas should notify him once ready for the corrected Ollama rerun.

## Sessions 15–20 (evening)

- Atlas fixed the `main` agent's model auth store directly so `openclaw models status --agent main` showed an `ollama:default` profile in `C:\Users\Admin\.openclaw\agents\main\agent\auth-profiles.json`.
- Test 5 confirmed the intended provider/model routing was finally correct: both Hermes and Chronos subagent histories showed `provider: ollama` and `model: qwen3:32b`.
- However, Test 5 also proved the remaining failures were no longer platform/auth-related: Hermes wandered into bad tool choices and Chronos failed to produce outputs.
- Gabriel updated both SKILL.md files. Atlas reviewed them and reran both agents.
- Test 6 isolated the next two concrete blockers:
  - Hermes had a filename assumption bug and looked for `04F ... Requirements.txt` while the real extracted file was `04F ... Requirements.pdf.txt`.
  - Chronos still depended on an exec/sandbox workflow assumption that was unsafe in this subagent environment.
- Atlas applied minimum SKILL.md fixes:
  - Hermes: added explicit file-handling rules to prefer actual `_extracted` filenames and not assume `.txt` names without `.pdf`.
  - Chronos: added execution rules to use plain `exec` only for `node generate.js`, with no host/security/pty/elevated/sandbox overrides and no config-change attempts.
- Test 7 improved behaviour again:
  - Hermes partially generated `06 Technical Proposal.docx` but not the full document set.
  - Chronos narrowed down to a generated JavaScript syntax error (extra `}` before a `new TextRun(...)` line).
- Key conclusion: we are out of model-routing/auth hell and now firmly in skill/prompt/generated-script debugging.

## Pending next
- Inspect generated JS/docx artifacts from Hermes and Chronos after the latest Ollama runs.
- Patch skill instructions based on the exact generated-script failure modes.
- Run a tighter Ollama test round after surgical fixes.
- Compare successful Ollama outputs against backed-up GPT-5.4 set.
- Jackie review of output quality before Daedalus.
